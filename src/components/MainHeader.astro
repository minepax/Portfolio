<header class="header">
    <div class="wrapper">
        
        <a href="/Portfolio" class="logo">PORTFOLIO</a>

        <nav class="navbar">
            <ul>
                <li><a class="navbar-link" href="/Portfolio">HOME</a></li>
                <li><a class="navbar-link" href="/Portfolio">MY WORK</a></li>
                <li><a class="navbar-link" href="/Portfolio" aria-label="Navbar Dropdown Menu Toggle" aria-controls="navbar-sub">HOBBIES</a>
                    <div class="navbar-sub" id="navbar-sub">
                        <ul>
                            <li><a class="navbar-sub-link" href='/Portfolio/badminton'>BADMINTON</a></li>
                            <li><a class="navbar-sub-link" href="/Portfolio">CUBING</a></li>
                            <li><a class="navbar-sub-link" href="/Portfolio">MINECRAFT</a></li>
                            <li><a class="navbar-sub-link" href="/Portfolio">CONSUMER TECH</a></li>
                        </ul>
                    </div>
                </li>
                <li><a class="navbar-link" href="/Portfolio">CONTACT</a></li>
            </ul>
        </nav>
        
        <button class="hamburger-btn" aria-label="Sidebar Toggle Buttton" aria-controls="sidebar">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 -960 960 960">
                <path d="M120-240v-80h720v80H120Zm0-200v-80h720v80H120Zm0-200v-80h720v80H120Z"/>
            </svg>
        </button>

    </div>
</header>

<nav class="sidebar" id="sidebar">
    <ul>
        <li><a class="sidebar-link" href="/Portfolio"><span>HOME</span></a></li>
        <li><a class="sidebar-link" href="/Portfolio"><span>MY WORK</span></a></li>
        <li>
            <button class="dropdown-btn" aria-label="Sidebar Dropdown Menu Toggle Button" aria-controls="sidebar-sub">
                <span class="sidebar-link">HOBBIES</span>
                <div class="arrow-down"></div>
            </button>
            <ul class="sidebar-sub" id="sidebar-sub">
                <div>
                    <li><a class="sidebar-sub-link" href="/Portfolio/badminton">BADMINTON</a></li>
                    <li><a class="sidebar-sub-link" href="/Portfolio">CUBING</a></li>
                    <li><a class="sidebar-sub-link" href="/Portfolio">MINECRAFT</a></li>
                    <li><a class="sidebar-sub-link" href="/Portfolio">CONSUMER TECH</a></li>
                </div>
            </ul>
        </li>
        <li><a class="sidebar-link" href="#"><span>CONTACT</span></a></li>
    </ul>
</nav>

<div class="overlay"></div>

<script src="/Portfolio/scripts/ui/sidebar.js" is:inline />

<style>

.header {
    font-family: "Montserrat", sans-serif;
    font-size: 1rem;
    font-weight: 500;
    position: sticky;
    top: -6rem;
    left: 0;
    z-index: 11;
    height: 5rem;
    animation: floating_nav linear forwards;
    animation-timeline: view();
    animation-range-start: 120vh;
    animation-range-end: 150vh;

    .wrapper {
        height: 100%;
        display: flex;
        justify-content: space-between;
    }

    .logo {
        font-size: clamp(1.5rem, 20vw  + 0.5rem, 1.8rem);
        display: var(--_display-none, flex);
        align-items: center;
        transition: color 0.5s ease;
        color: transparent;
        background: linear-gradient(
            90deg,
            var(--clr-grd-1),
            var(--clr-grd-2),
            var(--clr-grd-2),
            var(--clr-grd-1)
        );
        background-clip: text;
        background-size: 200% auto;
        animation: logo_animation 3s linear infinite;

        &:is(:hover, :focus) {
            color: var(--clr-accent-1);
            transform: none;
        }
    }
}

@keyframes logo_animation {
    0% {background-position: 200% 0%;}
    100% {background-position: 0% 0%;}
}

@keyframes floating_nav {
    0%, 100% {
        margin-inline: auto;
        padding-inline: 1rem;
        width: fit-content;
        height: 5rem;
        border-radius: 100vw;
        border: 1px solid hsl(from var(--clr-tertiary) h s l / 0.4);
        box-shadow: var(--shadow);
        background-color: var(--nav-alt-clr);
        backdrop-filter: blur(10px);
        --_display-none: none;
    }
    100% {
        top: 1rem;
    }
}

.navbar {

    ul {
        list-style-type: none;
        height: 100%;
        display: flex;
        gap: 2rem;

        li:is(:hover, :focus-within) .navbar-sub {height: auto; visibility: visible;}
    }
    
    .navbar-link {
        position: relative;
        height: 100%;
        cursor: pointer;
        display: flex;
        align-items: center;
        color: var(--clr-tertiary);
        transition: color 0.5s ease;
        
        &::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            width: 0;
            height: 2px;
            display: var(--_display-none, block);
            background-color: var(--clr-accent-1);
            transition: all 0.5s ease;
            transform: translateX(-50%);
        }
        
        &:is(:hover, :focus) {
            color: var(--clr-accent-1);
            
            &::after {
                width: 100%;
            }
        }
        
        &:has(+ .navbar-sub)::after {display: none;}
    }
}

.navbar-sub {
    position: absolute;
    height: 0;
    visibility: hidden;
    overflow: clip;
    interpolate-size: allow-keywords;
    border-radius: 0 0 1rem 1rem;
    background-color: var(--sidebar-sub-clr);
    transition: height 0.5s ease, visibility 0.5s;

    ul {
        list-style-type: none;
        display: grid;
        gap: 0;
    }

    .navbar-sub-link {
        padding: 1rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        color: var(--clr-tertiary);
        transition: color 0.5s ease;

        &:is(:hover, :focus) {color: var(--clr-accent-1);}
    }
}

/* ----- SIDEBAR ----- */

.sidebar {
    font-family: "Montserrat", sans-serif;
    font-size: 1rem;
    font-weight: 500;
    position: fixed;
    top: 0;
    z-index: 999;
    overflow: clip;
    width: 0;
    height: 100dvh;
    visibility: hidden;
    interpolate-size: allow-keywords;
    background-color: var(--nav-alt-clr);
    backdrop-filter: blur(10px);
    transition: width 0.5s ease, visibility 0.5s;

    &:is([data-visible], :focus-within) {width: max-content; visibility: visible;}

    > ul {
        list-style-type: none;
        padding: 2rem 1rem;
        width: 100%;
        height: 100%;
        overflow: clip auto;

        &::-webkit-scrollbar {
            width: 0.1rem;
            height: 0.2rem;
            background: var(--clr-primary);
        }

        &::-webkit-scrollbar-track {
            background: var(--clr-primary);
        }

        &::-webkit-scrollbar-track-piece {
            background: var(--clr-primary);
        }

        &::-webkit-scrollbar-thumb {
            background-color: var(--clr-accent-1);
        }
    }

    .sidebar-link {
        padding: 1.5rem;
        color: var(--clr-tertiary);
        text-wrap: nowrap;
        display: flex;
        align-items: center;
        transition: all 0.5s ease;

        &:is(:hover, :focus) {
            color: var(--clr-accent-1);
        }
    }
}

.dropdown-btn {
    padding-right: 1.5rem;
    width: 100%;
    cursor: pointer;
    text-align: left;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border: none;
    background: none;

    .arrow-down {
        width: 0px;
        height: 0px;
        border-inline: 6px solid transparent;
        border-top: 6px solid var(--clr-tertiary);
        transition: all 0.5s ease;

        &[data-rotate] {transform: rotate(-180deg);}
    }

    &:is(:hover, :focus) {
        .sidebar-link {color: var(--clr-accent-1);}
        .arrow-down {border-top: 6px solid var(--clr-accent-1);}
    }
}

.sidebar-sub {
    display: grid;
    grid-template-rows: 0fr;
    visibility: hidden;
    border-radius: 1rem;
    background-color: var(--clr-tertiary);
    transition: grid-template-rows 0.3s ease, visibility 0.3s;

    &[data-visible] {grid-template-rows: 1fr; visibility: visible;}

    div {overflow: hidden;}

    .sidebar-sub-link {
        padding: 1.5rem;
        color: var(--clr-primary);
        text-wrap: nowrap;
        display: flex;
        align-items: center;
    }
}

.overlay {
    background: hsl(0 0% 0% / 0.5);
    position: absolute;
    inset: 0;
    z-index: 997;
    display: none;
}

.sidebar[data-visible] ~ .overlay {display: block;}

/* ----- HAMBURGER BUTTON ----- */

.hamburger-btn {
    display: none;
    cursor: pointer;
    border: none;
    background-color: transparent;

    svg {
        width: clamp(2.5rem, 1vw  + 0.5rem, 3rem);
        fill: var(--clr-accent-1);
    }
}

/* ----- MEDIA QUERIES - NAV AND SIDE BARS ----- */

@media screen and (max-width: 800px) {
    .hamburger-btn {display: block;}

    .header {
        top: 0;
        animation: none;
        background-color: var(--nav-alt-clr);
        backdrop-filter: blur(10px);

        .navbar {
            display: none;
        }
    }
}

</style>